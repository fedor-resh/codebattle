# Бинарный поиск

Дан отсортированный по возрастанию массив `nums`. Верните индекс `target` или `-1`, если такого числа нет.

Реализуйте функцию `Solve(nums []int, target int) int` в пакете `solution`.
