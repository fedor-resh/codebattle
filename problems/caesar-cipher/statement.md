# Шифр Цезаря

Сдвиньте английские буквы в `text` вправо на `shift` позиций, сохраняя регистр; остальные символы не меняйте.

Реализуйте функцию `Solve(text string, shift int) string` в пакете `solution`.
