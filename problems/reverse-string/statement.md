# Разворот строки

Верните строку в обратном порядке. Строка может содержать Unicode-символы.

Вход: исходная строка. Выход: перевёрнутая строка.

Реализуйте функцию `Solve(input string) string` в пакете `solution`.
