# Два слагаемых

Вход имеет вид `цель|числа через запятую`. Верните индексы первой найденной пары `i,j`, сумма которой равна цели. Гарантируется одно решение.

Реализуйте функцию `Solve(input string) string` в пакете `solution`.
