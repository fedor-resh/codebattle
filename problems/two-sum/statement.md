# Два слагаемых

Верните индексы первой найденной пары в `nums`, сумма которой равна `target`. Гарантируется одно решение.

Реализуйте функцию `Solve(nums []int, target int) []int` в пакете `solution`.
