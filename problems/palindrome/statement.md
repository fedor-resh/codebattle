# Палиндром

Определите, читается ли строка одинаково слева направо и справа налево. Регистр и все символы учитываются.

Выход: `true` или `false`.

Реализуйте функцию `Solve(input string) string` в пакете `solution`.
