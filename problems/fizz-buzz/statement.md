# FizzBuzz

Дано целое число `n`. Сформируйте значения от 1 до n через запятую: кратные 3 замените на `Fizz`, кратные 5 — на `Buzz`, кратные обоим — на `FizzBuzz`.

Реализуйте функцию `Solve(input string) string` в пакете `solution`.
