# FizzBuzz

Дано целое число `n`. Верните значения от 1 до n: кратные 3 замените на `Fizz`, кратные 5 — на `Buzz`, кратные обоим — на `FizzBuzz`.

Реализуйте функцию `Solve(n int) []string` в пакете `solution`.
