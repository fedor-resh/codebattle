# Анаграммы

Вход имеет вид `первая|вторая`. Определите, состоят ли строки из одинакового набора Unicode-символов с учётом регистра. Верните `true` или `false`.

Реализуйте функцию `Solve(input string) string` в пакете `solution`.
