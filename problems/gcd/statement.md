# Наибольший общий делитель

На вход подаются два неотрицательных целых числа через запятую. Верните их наибольший общий делитель.

Реализуйте функцию `Solve(input string) string` в пакете `solution`.
