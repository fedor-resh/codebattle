# Самое длинное слово

Найдите первое самое длинное слово во входной строке. Слова разделены пробелами, длина считается в Unicode-символах.

Реализуйте функцию `Solve(text string) string` в пакете `solution`.
