# Слияние массивов

Вход имеет вид `первый массив|второй массив`; числа внутри массива разделены запятыми. Слейте два отсортированных массива.

Реализуйте функцию `Solve(input string) string` в пакете `solution`.
