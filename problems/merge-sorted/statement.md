# Слияние массивов

Даны два отсортированных массива `left` и `right`. Верните новый отсортированный массив со всеми элементами.

Реализуйте функцию `Solve(left, right []int) []int` в пакете `solution`.
