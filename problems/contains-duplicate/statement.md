# Есть ли дубликаты

Дан массив целых чисел `nums`. Верните `true`, если хотя бы одно значение встречается больше одного раза. Если все значения уникальны, верните `false`.

Реализуйте функцию `Solve(nums []int) bool` в пакете `solution`.
