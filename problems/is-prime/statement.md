# Простое число

Дано целое число `n`. Верните `true`, если `n` является простым, иначе верните `false`.

Простое число больше `1` и делится без остатка только на `1` и само себя.

Реализуйте функцию `Solve(n int) bool` в пакете `solution`.
