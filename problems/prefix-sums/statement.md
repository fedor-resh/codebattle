# Префиксные суммы

Дан массив целых чисел `nums`. Верните массив той же длины, где элемент с индексом `i` равен сумме элементов `nums` от индекса `0` до `i` включительно.

Для пустого массива верните пустой массив.

Реализуйте функцию `Solve(nums []int) []int` в пакете `solution`.
